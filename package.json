{
  "name": "okul-takip-sistemi",
  "version": "1.0.0",
  "description": "Okul Takip Sistemi",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "postinstall": "pip3 install -r requirements.txt || pip install -r requirements.txt || python3 -m pip install -r requirements.txt || python -m pip install -r requirements.txt || echo 'Pip installation bypassed'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
