{
  "name": "okul-takip-sistemi",
  "version": "1.0.0",
  "description": "Okul Takip Sistemi",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "postinstall": "python3 -m pip install --break-system-packages -r requirements.txt || pip3 install --break-system-packages -r requirements.txt || python3 -m pip install --user -r requirements.txt || pip install -r requirements.txt || echo 'Python pip step optional'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1",
    "xlsx": "^0.18.5"
  }
}
