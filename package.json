{
  "name": "okul-takip-sistemi",
  "version": "1.0.0",
  "description": "Okul Takip Sistemi",
  "main": "server.mjs",
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "postinstall": "python3 -m pip install -r requirements.txt || (curl -sS https://bootstrap.pypa.io/get-pip.py -o get-pip.py && python3 get-pip.py --user && ~/.local/bin/pip3 install --user -r requirements.txt) || echo 'Pip fallback failed'"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "pdf-parse": "^1.1.1",
    "xlsx": "^0.18.5"
  }
}
